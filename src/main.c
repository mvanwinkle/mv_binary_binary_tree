#include <config.h>
#include <stdio.h>

#include "mv-binary-binary-tree.h"

int main(int argc, char *argv[])
{
	puts ("Hello world from " PACKAGE_STRING);
	mv_binary_binary_tree_hello();
	return 0;
}
