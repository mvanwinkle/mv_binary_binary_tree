#ifndef mv_binary_binary_tree__
#define mv_binary_binary_tree__

extern void mv_binary_binary_tree_hello(void);

#endif // mv_binary_binary_tree__

// Compile this with:
//
// gcc -c -Wall -Werror -fpic hello_ias.c
// gcc -shared -o libhello_ias.so hello_ias.o
